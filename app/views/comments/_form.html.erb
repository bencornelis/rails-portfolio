<%= form_for [@post, @comment], remote: true do |f| %>
  <div class="form-group">
    <%= f.text_area :text, class: "form-control", rows: "5", placeholder: "Write your comment here." %>
  </div>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
